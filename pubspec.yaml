name: riddance_env
version: 0.0.21
description: Automate Flutter maintenance chores.
topics:
  - devenv
  - riddance
  - codegen
  - template
  - build-runner

environment:
  sdk: ^3.8.1

dependencies:
  icons_launcher: 3.0.1
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: 6.0.0
